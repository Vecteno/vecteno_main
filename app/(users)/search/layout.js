export const metadata = {
  title: 'Search Results - Vecteno',
  description: 'Vecteno.com',
}

export default function SearchLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}
